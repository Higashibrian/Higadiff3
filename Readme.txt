Higadiff is a visual comparison tool which uses selenium webdriver 
to build a set of images from sites with two different domains but
 otherwise identical paths. The intent is to catch any minor or 
major visual differences between two environments

Running the program will open a CLI 